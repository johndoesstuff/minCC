float a = 0.41;
printf("assigned %f to a\n", a);
float b = 1e-2;
printf("assigned %f to b\n", b);
float c = a + b;
printf("assigned %f (a + b) to c\n", c);
c = c * c;
printf("assigned %f (c^2) to c\n", c);
return 0;
